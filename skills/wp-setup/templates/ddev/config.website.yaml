# .ddev/config.yaml — ClaudePress website branch
# Rendered by wp-setup; {{SLUG}} is substituted from resolved-config.json.
name: "{{SLUG}}"
type: wordpress
docroot: web
php_version: "8.3"
database:
  type: mariadb
  version: "10.11"
webserver_type: nginx-fpm
