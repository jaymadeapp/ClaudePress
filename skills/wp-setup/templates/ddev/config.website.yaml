# .ddev/config.yaml — ClaudePress website branch
# Rendered by wp-setup; {{SLUG}} and {{PHP_VERSION}} are substituted from resolved-config.json.
name: "{{SLUG}}"
type: wordpress
docroot: web
php_version: "{{PHP_VERSION}}"
database:
  type: mariadb
  version: "10.11"
webserver_type: nginx-fpm
