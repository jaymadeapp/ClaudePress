{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp@latest"]
    },
    "wordpress": {
      "command": "ddev",
      "args": ["wp", "mcp-adapter", "serve", "--server=mcp-adapter-default-server", "--user=claudepress-mcp"]
    }
  },
  "_setup": "Local WordPress MCP uses STDIO via WP-CLI as the user `claudepress-mcp` — NO application password, NO secrets in this file. The default above is the DDEV/Docker form (recommended/required for an e-shop). For a no-Docker setup, change the wordpress server to {\"command\":\"wp\",\"args\":[\"mcp-adapter\",\"serve\",\"--server=mcp-adapter-default-server\",\"--user=claudepress-mcp\"]}. The mcp-adapter plugin install + the least-privilege `claudepress-mcp` user are provisioned automatically by `scripts/setup-mcp.sh` (requires WP >=6.9 / Abilities API, PHP >=7.4). E-SHOP SECURITY: role `claudepress_mcp` is CONTENT-ONLY and MUST NOT have WooCommerce order/payment capabilities (no manage_woocommerce, no edit_shop_orders, no order read/refund caps) — the MCP client acts AS this user, so its role is the security boundary; orders, customers and payment data must never flow through the agent. For a REMOTE/production site, do NOT use STDIO — use the HTTP-proxy + Application Password variant instead (@automattic/mcp-wordpress-remote with WP_API_URL/USERNAME/PASSWORD), documented in reference/docker.md and reference/no-docker.md. The autonomous agent works against LOCAL only, never prod."
}
